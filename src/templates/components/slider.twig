{#
Section with slider

@param {array}  items {  
    @param {string} image
    @param {straing} text
    @param {object} cta {
        @param {string} title
        @param {string} link
    }
}
#}

{# Output #}
